import styles from "./button.module.css";

const Button = () => {
    return (
        <button className={styles.splitButton}>
            START SHOPPING
        </button>
    )
}

export default Button